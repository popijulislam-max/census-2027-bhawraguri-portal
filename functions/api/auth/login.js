import {json,body,sign} from '../_utils.js';
export async function onRequestPost({request,env}){const {username,password}=await body(request);if(username!==env.ADMIN_USERNAME||password!==env.ADMIN_PASSWORD)return json({error:'Invalid credentials'},401);const payload=btoa(JSON.stringify({u:username,t:Date.now()})).replace(/=+$/,'');const token=payload+'.'+await sign(payload,env.ADMIN_SESSION_SECRET);return new Response(JSON.stringify({ok:true}),{headers:{'content-type':'application/json','Set-Cookie':`census_admin=${token}; HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=28800`}})}
